# Copy to `.env` in this directory and fill in values (`.env` is gitignored).
# Loaded automatically when v3 code calls `load_config()` if python-dotenv is installed.
#
# Names must match v3/config.yaml kraken.api_key_env / api_secret_env (defaults below).

KRAKEN_API_KEY=
KRAKEN_API_SECRET=
