# Copy to /etc/mft-cashcow.env (or another path) and chmod 600.
# Reference from systemd: EnvironmentFile=-/etc/mft-cashcow.env
#
# Names must match v3/config.yaml kraken.api_key_env / api_secret_env (defaults below).

KRAKEN_API_KEY=
KRAKEN_API_SECRET=
